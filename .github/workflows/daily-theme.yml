name: Daily profile theme

on:
  schedule:
    # 00:10 UTC every day
    - cron: "10 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-theme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply odd/even day theme
        run: python scripts/apply_theme.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          if git diff --staged --quiet; then
            echo "Theme already up to date."
          else
            DAY=$(date -u +%d)
            MODE=$(python -c "from datetime import datetime, timezone; d=datetime.now(timezone.utc).day; print('dark' if d%2 else 'light')")
            git commit -m "chore: switch profile README to ${MODE} theme (UTC day ${DAY})"
            git push
          fi
